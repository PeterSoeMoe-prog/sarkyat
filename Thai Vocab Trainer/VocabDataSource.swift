import Foundation

// Disabled duplicate loader; the canonical `loadCSV()` lives in ContentView.swift.
// Keeping this file as a placeholder to avoid breaking any file references.
#if false
func loadCSV() -> [VocabularyEntry] { [] }
#endif
