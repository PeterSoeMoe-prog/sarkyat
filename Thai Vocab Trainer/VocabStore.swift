import Foundation

// Placeholder to keep the file compiling until we finish the shared-store refactor.
// The app currently uses the existing ContentView-based data flow.
#if false
final class VocabStore: ObservableObject {}
#endif
